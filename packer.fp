// enter the file names here
src("filename1",
	"filename2",
	"filename3");
// The file which contains the main function
main("filename1");

destination("/dist");
lactro (comedy side role);
spire (villan);
vantaker (opertunist);